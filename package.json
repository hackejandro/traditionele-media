{
  "name": "traditionele-media",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vinext dev",
    "build": "vinext build",
    "start": "vinext start",
    "lint": "eslint . --ignore-pattern dist --ignore-pattern .next --ignore-pattern .worker-dist --ignore-pattern worker/worker-configuration.d.ts",
    "worker:types": "wrangler types --config worker/wrangler.jsonc worker/worker-configuration.d.ts",
    "worker:check": "wrangler deploy --dry-run --config worker/wrangler.jsonc --outdir .worker-dist",
    "worker:dev": "wrangler dev --config worker/wrangler.jsonc",
    "worker:deploy": "wrangler deploy --config worker/wrangler.jsonc",
    "analytics:report": "cd /private/tmp && npm exec --yes --package=wrangler@4.135.0 -- wrangler d1 execute HISTORY_DB --remote --config \"$INIT_CWD/worker/wrangler.jsonc\" --command \"SELECT event_date AS day, COUNT(*) AS pageviews, COUNT(DISTINCT analytics_events.visitor_id) AS unique_browsers, COUNT(DISTINCT CASE WHEN first_seen_date = event_date THEN analytics_events.visitor_id END) AS new_browsers, COUNT(DISTINCT CASE WHEN first_seen_date < event_date THEN analytics_events.visitor_id END) AS returning_browsers FROM analytics_events JOIN analytics_visitors USING (visitor_id) WHERE event_type = 'pageview' GROUP BY event_date ORDER BY event_date DESC LIMIT 31; SELECT event_date AS day, page_path, COUNT(*) AS pageviews, COUNT(DISTINCT visitor_id) AS unique_browsers FROM analytics_events WHERE event_type = 'pageview' GROUP BY event_date, page_path ORDER BY event_date DESC, pageviews DESC LIMIT 100; SELECT event_date AS day, COALESCE(conversation_title, conversation_url) AS conversation, conversation_url AS url, COUNT(*) AS opens, COUNT(DISTINCT visitor_id) AS unique_browsers FROM analytics_events WHERE event_type = 'conversation_open' GROUP BY event_date, conversation_url ORDER BY event_date DESC, opens DESC LIMIT 100;\""
  },
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "1.37.1",
    "@cloudflare/workers-types": "^5.20260922.1",
    "@openai/sites-vite-plugin": "^0.1.0 || ^0.2.0",
    "@tailwindcss/postcss": "4.2.1",
    "@types/node": "22.19.19",
    "@types/react": "19.2.14",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.2",
    "@vitejs/plugin-rsc": "0.5.26",
    "eslint": "9.39.4",
    "eslint-config-next": "16.2.6",
    "react-server-dom-webpack": "19.2.6",
    "tailwindcss": "4.2.1",
    "typescript": "5.9.3",
    "vinext": "1.0.0-beta.3",
    "vite": "8.0.13",
    "wrangler": "^4.135.0"
  },
  "type": "module"
}
